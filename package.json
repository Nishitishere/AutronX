{
  "name": "autronx-panel-builder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AutronX smart switch panel configurator",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check auth-store.mjs && node --check script.js && node --check ui.js && node --check account.js && node --check dashboard.js && node --check super-admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  }
}
